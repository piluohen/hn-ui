```HTML
<hn-table
  #table
  size="default"
  [columns]="columns"
  [data]="tableData"
  [params]="{}"
  [draggable]="true"
  draggDisabledKey="disabled"
  (draggChange)="handleDraggChange($event)">
</hn-table>
```
